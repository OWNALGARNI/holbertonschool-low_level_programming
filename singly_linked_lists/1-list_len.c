#include <stdio.h>
#include <stdlib.h>

typedef struct list_s
{
char *str;
int count;
struct list_s *next;
}
list_t;

size_t list_len(const list_t *h)
{
while (list != NULL)
{
printf("%s\n", current->str);
current = current->next;
count++;
}
return (count);
}
